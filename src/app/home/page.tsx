import LogoutButton from "@/components/auth/LogoutButton";

const HomePage = () => {
  return (
    <div className="home">
      HomePage
      <LogoutButton />
    </div>
  );
};

export default HomePage;
